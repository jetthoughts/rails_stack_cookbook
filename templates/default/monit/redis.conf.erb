check process redis with pidfile <%= node[:redis][:pid_file] %>
    start program = "/etc/init.d/redis-server start"
    stop program = "/etc/init.d/redis-server stop"
    group redis
    if mem > 400 MB for 5 cycles then alert
